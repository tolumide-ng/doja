#[derive(Debug)]
pub(crate) struct Signal { // this is a planned rewrite of the clock currently called 'Controller'
    // start_time: u32,
    // stop_time: u32,
    // pub(crate) depth: u8,
    // time_set: u32,
    // moves_togo: u8,
    
}